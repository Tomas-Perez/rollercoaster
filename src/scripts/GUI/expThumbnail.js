function ExpThumpbnail(path, parent, number) {
    this.imagePath = imagePath
    this.div = createDiv('');
    this.div.attribute('id', 'expImgDiv' + number);
    this.div.addClass('expImgDiv');
    this.div.parent(parent);
    //TODO
}
